

public class GUI {
    public static void main(String[] args) {
        /*

        JFrame frame = new JFrame();
        frame.setTitle("Dipu Singha dedicated this for Rosmi Sinha for his love");
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.setResizable(true);
        frame.setSize(900, 500);
        frame.getContentPane().setBackground(new Color(150, 130, 100)); // custom color
        //frame.getContentPane().setBackground(new Color(100, 0, 100)); // custom color

        // frame.getContentPane().setBackground(Color.gray); // default color
        frame.setVisible(true);
        ImageIcon image = new ImageIcon("helloWorld.png");
        frame.setIconImage(image.getImage());
        */
        //RosmiFrame myFrame = new RosmiFrame();
        new RosmiFrame(); // alternative to the previous line of code
        
    }

}
